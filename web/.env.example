# Copia este archivo a web/.env.local (no subas .env.local al repositorio).
# Usa los mismos valores que en mobile/.env (mismo proyecto Supabase).

NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_anon_key_aqui
