import { View } from "react-native";

/** La navegación la resuelve `app/(board)/_layout.tsx` vía `router.replace`. */
export default function BoardIndexPlaceholder() {
  return <View style={{ flex: 1 }} />;
}
